{
  "name": "2019-10-23-fast-typescript-react",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": "12.13.0"
  },
  "scripts": {
    "dev": "run-p dev:api dev:ui",
    "dev:api": "yarn workspace api dev",
    "dev:ui": "yarn workspace ui dev",
    "start": "yarn workspace api start",
    "prebuild": "run-s env:local:init env:validate",
    "build": "yarn workspace ui build",
    "env:local:init": "env/initLocal",
    "env:validate": "node -r esm env"
  },
  "workspaces": [
    "api",
    "ui",
    "env",
    "e2e"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
